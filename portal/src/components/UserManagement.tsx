"use client"

import { useEffect } from "react";

export default function UserManagement() {
    useEffect(() => {
    }, [])

    return (
        <main className="flex flex-row items-center justify-center text-black bg-white h-full w-full">

        </main>
    );
}
